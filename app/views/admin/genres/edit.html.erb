<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for [:admin, @genre] do |f| %>
      <%= render "shared/error_messages", object: @genre %>

      <%= f.label :name %>
      <%= f.text_field :name, class: "form-control" %>

      <%= f.submit t("new"), class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
