<%= form_for(@comment ||= Comment.new, remote: true) do |f| %>
  <div class="comment-form new_comment_box">
    
      <%= f.hidden_field :movie_id, value: movie.id %>
        <%= f.text_area :content, rows: 6, placeholder: 'Add a comment...', class: 'form-control form-margin' %>
        <br/>
        <%= f.submit "Comment", class: "commentbutton" %>
    </div>
  </div>
<%end%>

